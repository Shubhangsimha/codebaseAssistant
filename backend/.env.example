# Copy this file to .env and fill in your API keys.
# Only GEMINI_API_KEY and GROQ_API_KEY are required.

GEMINI_API_KEY=
GROQ_API_KEY=

# Directory where SQLite DB and FAISS indexes are stored.
# Leave as default unless running outside Docker.
DATA_DIR=./data
